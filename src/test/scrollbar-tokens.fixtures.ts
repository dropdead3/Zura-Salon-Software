// Deprecated: prefer `src/test/css-rule.ts` for new CSS-canon tests.
// Kept as a re-export shim so existing tests keep working without edits.
export { extractRuleBody, readIndexCss } from "./css-rule";
