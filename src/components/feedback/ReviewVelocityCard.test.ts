import { describe, it, expect } from 'vitest';
import { ReviewVelocityCard } from './ReviewVelocityCard';

describe('ReviewVelocityCard', () => {
  it('exports a component', () => {
    expect(typeof ReviewVelocityCard).toBe('function');
  });
});
